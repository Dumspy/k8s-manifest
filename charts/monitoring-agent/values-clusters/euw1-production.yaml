# Cluster-specific overrides for euw1-production cluster
# Applied after values.yaml and values-stages/production.yaml

global:
  clusterName: "euw1-production"
  environment: "production"
  hubEndpoint: "100.64.0.2"  # Replace with actual hub Tailscale IP

# Cluster-specific resource adjustments
alloy:
  resources:
    limits:
      memory: "1.5Gi"  # Larger cluster, higher limits

# Cluster-specific collectors
# alloy.configMap.content: |
#   // Add cluster-specific metrics collection
#   prometheus.scrape "region_specific" {
#     targets = [...]
#   }
